---
name: 女频爱情-设计总大纲
description: '用于【女频爱情】题材下的总大纲设计任务。作为题材包装层、兼容入口与路由层，负责保留女频爱情入口，补充情绪锚定、成长线优先、关系拉扯、柔爽感与现实共鸣边界，并明确要求优先强制加载并使用 `通用-设计总大纲`。'
argument-hint: '要设计或重写哪份女频爱情总大纲？默认按“成长线优先、情绪承诺、关系长线与阶段兑现”处理。'

user-invocable: false
---


<!-- ===== Layer 1: 永久缓存 ===== -->

# 女频爱情-设计总大纲

这是题材包装层、兼容入口与路由层。


<!-- ===== Layer 3: 场景缓存 ===== -->

## 对应通用 Skill

- `通用-设计总大纲`

## 本层职责

- 保留“女频爱情-设计总大纲”这一题材入口名。
- 将该能力的共性骨架路由到 `通用-设计总大纲`。
- 补充女频爱情题材在“总大纲设计”上的情绪锚定、成长线优先、关系拉扯、柔爽感与现实共鸣边界。
- 在补写本题材 Skill 时，持续识别新增内容里哪些其实属于题材无关的通用能力，并将其回收到 `通用-设计总大纲` 或对应通用 Skill。

## 题材补充边界

- 设计时默认先回答：这条线怎样让读者在前 300 字看见情绪痛点，并在前三章看到第一次有效回报。
- 默认要求“成长线 > 爱情线”，但关系线必须持续供血成长，而不是只做陪衬。
- 女主与关键角色都要有真实短板、代价与选择压力，拒绝无摩擦悬浮设定。
- 必须把“被轻慢、被误解、被放弃、被看见、被坚定选择”等情绪锚点压进剧情动作。
- 允许无 CP、双强、事业线先行或题材融合，但都必须让情绪价值与读者代入感成立。

## 建议组合调用

- `通用-设计总大纲`

## 强制要求

- 命中本技能时，**必须优先强制加载**当前题材 Skill 与 `通用-设计总大纲`。
- 当前题材 Skill 负责女频爱情题材的边界、补充口径与裁判要求，`通用-设计总大纲` 负责共性能力本体与通用流程。
- 不得绕过 `通用-设计总大纲` 在本文件里恢复一整套平行共性规则。
- 引用通用能力时只按名称引用，不写路径。
- 若在编写或修改本题材 Skill 时新增了题材无关的规则、流程、模板或检查项，必须同步提取并沉淀到 `通用-设计总大纲` 或对应通用 Skill。

## 禁止事项

- 不要只给题材标签、情绪口号或设定摘要，而不给正文可调用的动作链。
- 不要把女频爱情主线设计成“谁爱谁”单线条，而缺失成长、选择与代价。
- 它指的是一个互相联动的**总纲层文件组**，至少包括：
## 继续读取的 references（强制读取门禁）

**以下所列 references 文件必须通过 `read_file` 工具逐文件读取，不得因"已有相关知识"、"之前执行时已读过"、"该文件只是参考"为理由跳过任何一条。** 每条 references 按其标注的必读等级强制执行：

- 标注 **必读** 的文件：必须读取，少一条即视为流程违规，不得开始执行流程。
- 未标注必读的文件：必须读取，但读取后可仅提取与当前任务直接相关的段落，不要求逐字通读。
- 若文件路径指向的文件不存在（如引用的外部路径未就位），必须在日志中显式记录 `reference_missing_{refName}`，不得静默跳过。

> 以下 references 为当前 skill 目录下的补充文件，**强制读取**。
- 
references/故事面联动首卷承诺与平台闭环.md — 故事面联动、首卷承诺与平台闭环
- 
references/总纲模板与验收.md — 总纲模板与验收补充（女频爱情）
- 
references/总纲专属设计规则.md — 总纲专属设计规则
