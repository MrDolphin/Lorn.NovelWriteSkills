---
name: 通用-大纲审阅优化闭环
description: '用于对指定提纲执行"审阅→修改→复审"的闭环，直到双轴评分（技法分 ≥ 6.0 / 留存分 ≥ 5.0）加权的综合评分连续两轮独立审阅在保留两位小数后均严格大于 9.20。适合大纲审阅优化 SOP、报告复用、回炉循环、评分门槛控制与最终回执收口。关键词：大纲审阅闭环、回炉到 9.20 以上、双轴评分、复审循环、主报告覆盖、提纲优化 SOP。'
argument-hint: '给我目标提纲路径和对象类型（总纲 / 分部 / 分卷 / 单元案）。结束条件：双轴各达标（技法≥6.0/留存≥5.0）且综合评分连续两轮严格大于 9.20。'

user-invocable: false
---

# 通用-大纲审阅优化闭环
> **题材路由**：若 .github\题材专用Skills\ 目录存在对应的 <题材>-大纲审阅优化闭环 Skill，则：
> - 将题材特性骨架路由到 <题材>-大纲审阅优化闭环，该 Skill 位于 .github\题材专用Skills\ 目录。
> - 命中本技能时，必须优先强制加载当前 Skill 与 <题材>-大纲审阅优化闭环。


> **头号要求：禁止吹捧，客观分析。** 闭环中调用的所有审阅环节均不得出现无依据的称赞、吹嘘性描述或主观美化。所有判断必须基于文本事实、可验证标准与明确扣分依据，优先指出问题与改进方向。

## 执行安保（硬性门禁）

### 阅读确认（强制）

**本文件不是摘要或参考提示，而是必须逐字执行的流程指令。**

在产生任何输出之前，必须先逐条输出以下确认。不输出确认就输出审阅结论，一律视为流程违规：

```
[阅读确认]
- 本文件 SKILL.md → [已完整读取，完全理解全部硬规则与强制要求]
- references/大纲审阅优化闭环总流程.md → [已读取并理解]
- references/审阅报告复用与9点2门槛.md → [已读取并理解]
```

若项目根目录存在 Agents.md，再追加：

```
- Agents.md → [已读取，已识别主输出平台 / 注册模板]
```

### 执行器定位（硬性）

- **本 Skill 是"执行器"，不是"规划器"**：目标是把闭环真实跑完，而不是输出计划或阶段总结。
- **一气呵成**：一旦开始执行，必须一气跑完步骤 0→6，中途不得暂停去询问可由既有规则推断的问题。
- **步进式输出**：每一步开始时输出 `[进度] 步骤 N：当前动作描述`。

### 阶段推进闸门（硬性）

- 步骤 1→3→4 构成首轮审阅—修改—复审的基础闭环，不得跳过任何一步。
- 每完成一个步骤，必须立即判定 nextStep 并继续推进。
- 步骤 2（综合评分读取）不是独立的收尾点——即使评分 > 9.2，只要尚未连续两轮过线，就必须继续。

### 禁止误判完成的情形（硬性）

1. **禁止把"首轮审阅已完成"当作"闭环完成"** — 只要未达到连续两轮 > 9.2，都不是完成。
2. **禁止把"文件已存在"代替"步骤已执行"** — 以本次执行中是否实际读取、修改、覆盖写回为准。
3. **禁止跳过基于审阅报告的逐条精修直接进复审** — 不改稿的复审视为无效复审。
4. **禁止把"接近 9.2"表述成"已达 9.2+"** — 9.2 算未达标，必须严格大于 9.2。
5. **禁止第二轮独立复审只查旧问题不查新问题** — 必须对当前修订版执行完整从严审阅。
6. **禁止以"与上一轮相比有进步"替代独立的"当前版本是否达标"判定。**

## 继续读取的 references

- `references/大纲审阅优化闭环总流程.md` — **必读**：执行顺序与对象路由
- `references/审阅报告复用与9点2门槛.md` — **必读**：报告复用策略与双轴评分门槛
- `../../写作研究/网文留存模型.md` — **必读**：留存评分共享基线，定义双轴门槛中的留存分基准与平台权重配置

## 风格/蓝本/写作研究模板自动发现规则

若当前服务的项目根目录存在 Agents.md，启动闭环前必须：

1. 读取项目根目录的 Agents.md；若其中声明了主输出平台，检索三类模板时优先匹配"适用平台"与主输出平台一致的模板，无匹配时回退到"适用平台：默认"的同类模板
2. 若 Agents.md 中注册了"写作研究模板"——读取对应路径的模板文件，将其作为题材×平台的默认评分基线
3. 若 Agents.md 中注册了"作者风格模板"——读取对应路径的模板文件，将其作为文风执行一致性审计依据
4. 若 Agents.md 中注册了"作品蓝本模板"——读取对应路径的模板文件，将其作为结构回炉目标，传递给审阅与修改环节
5. 本 Skill 作为编排器主要负责读取、记录、传递三模板约束；具体字段由被调用的审阅/设计 Skill 消费
6. 若项目根目录不存在 Agents.md，或其中未注册对应模板——回退通用默认模式，不报错，不阻断

## 主输出平台自动发现规则

若当前服务的项目根目录存在 Agents.md 且其中声明了主输出平台，启动闭环前必须：

1. 读取项目根目录的 Agents.md
2. 按命名约定定位对应的通用-输出{平台名}版 Skill
3. 将平台 Skill 的门禁标准作为本轮闭环的默认门禁基线
4. 在日志中记录主输出平台
5. 若未配置主平台或配置缺失——回退平台无关的通用默认模式，不报错，不阻断

## 题材包装层路由规则

题材目录下的包装层 Skill 是本 Skill 的题材特化扩展。调用规则：

- 在调用任一通用 Skill 之前，先检查当前项目题材目录下的 .github/skills/ 中是否存在同名的题材包装层
- 若存在：**先加载题材包装层 Skill**，再由其路由到通用 Skill 叠加题材规则
- 若不存在：直接调用通用 Skill，不报错，不阻断

## 对象路由规则

- **总纲层**（小说总大纲.md / 总纲包）：审阅调用 `通用-审阅总大纲`（及对应的题材包装层）；修改调用 `通用-设计总大纲`（及对应的题材包装层）
- **分部/分卷/单元案层**（分卷大纲 / 章清单等）：审阅调用 `通用-审阅分卷大纲`（及对应的题材包装层）；修改调用 `通用-设计分卷大纲`（及对应的题材包装层）

## 默认模型温度参考

| 阶段 | 推荐温度 | 说明 |
|---|---|---|
| 审阅轮 | 0.3 | 低创造性，保持判断稳定一致 |
| 修改轮 | 0.45 | 方向已定，适度约束 |
| 复审轮 | 0.3 | 低创造性，保持判断稳定一致 |

## 大纲审阅优化闭环流程

### 步骤 0：初始化与环境检查

**调用 skill：** 无（编排器自执行）；按需调用 `通用-分析竞对作品`

**扮演角色：** 编排器 / 项目经理角色，不代入任何审阅或设计视角，只做环境检查、路径推导与前置资料补齐。

**职责：** 识别任务对象，读取前置资料（上级文件、人物传记、故事设定等），补齐竞对基线，定位主审阅报告，创建目录结构，初始化执行日志。

| 子步骤 | 动作 | 产出 |
|---|---|---|
| 0.0 | 读取项目根目录 Agents.md，识别主输出平台与注册的三类模板 | 确定评分基线、模板约束 |
| 0.1 | **识别任务对象**：确认目标提纲文件路径与对象类型（总纲/分部/分卷/单元案） | 对象类型确认 |
| 0.2 | 读取目标提纲文件，确认当前版本与结构完整性 | 提纲现状掌握 |
| 0.3 | **背景资料阅读**：若对象为分卷/单元案，读取其上级分配文件（总纲/分部）确认分配内容是否落地；读取相关人物传记、故事设定与世界观文档作为审阅裁判源 | 裁判源就绪 |
| 0.4 | **竞对基线检查**：若题材目录下存在竞对分析/，读取并提炼竞对对照基线（领先点/落后点/补丁方向） | 竞对结论集 |
| 0.5 | 若目标平台为起点且题材目录下尚无可用竞对主报告，调用通用-分析竞对作品补齐最小结论集（Top10候选池+3-5本主样本+四层深拆速览）；若任务限制无法补齐，显眼记录盲区 | 竞对基线就绪 |
| 0.6 | **定位或创建主审阅报告**：在审阅意见/下搜索与提纲对象类型、名称或编号匹配的既有报告；若存在则锁定为当前主报告（取最新日期），若不存在则按命名规则创建 | 主报告路径确认 |
| 0.7 | 创建日志目录（SOP执行日志/大纲审阅/），初始化执行日志 | 日志就绪 |

**完成标记：** checkpoint.nextStep = 1，写入日志。

---

### 步骤 1：首轮审阅（强制完整模板）

**调用 skill：** `通用-审阅总大纲` 或 `通用-审阅分卷大纲`（及对应的题材包装层），按**对象路由规则**选择。

**扮演角色：** 审阅者角色（参考 `小说审阅者.agent.md` 的审阅立场）。**不得代入作者情绪**，以独立审阅者视角对提纲进行全面检查。审阅依据必须来自：提纲自身 + 上级分配文件 + 竞对基线 + 模板约束 + 人物传记/故事设定，不得以"我当初设计的时候是这么想的"为由降低标准。

**职责：** 对目标提纲进行全面审阅，产出完整审阅报告并落盘到主报告路径。

**【强制门禁：审阅报告模板完整性检查】**
审阅报告写入后、宣告本步骤完成前，必须逐项验证报告是否包含以下全部板块：

```
□ 阻断点/风险评估
□ 具体修改建议
□ 双轴达标确认（技法分≥6.0 / 留存分≥5.0）
□ 冲击 9.2+ 的优化思路
□ 综合评分：X.X/10（标注平台权重配置）
□ （若有竞对资料）统一竞对量化模块（含留存竞对比对）
□ （若有竞对资料）竞对借鉴点、我方落后点、优先优化动作
```

任一板块缺失 → **判定为简版报告**，必须回退到步骤 1.4 重新撰写完整审阅报告，不得带缺失进入步骤 2。

1. 按对象路由规则加载对应的审阅 Skill（及题材包装层），代入审阅者角色
2. 读取目标提纲全文 + 上级分配内容（若是分卷，需同时读总纲对应部分）+ 第三步中收集的相关人物传记/故事设定
3. 读取竞对分析资料，将其纳入审阅判断：至少写明竞对借鉴点、我方落后点、优先优化动作
4. 执行全面审阅，严格使用对应审阅 Skill 的完整报告模板
5. 报告中必须包含两行可直接解析的评分字段：
   - `- 技法分：X.X/10`
   - `- 留存分：X.X/10`
   - `- 综合评分：X.X/10（权重：技法W_+留存W_=平台名）`
6. 报告中必须包含：阻断点/风险、具体修改建议、冲击 9.2+ 的优化思路
7. 若存在竞对分析资料，报告中还必须包含统一竞对量化模块
8. 审阅报告**覆盖写入**主报告文件路径
9. 执行【审阅报告模板完整性检查】，确认无缺失后进入完成标记

**文件写入：** 审阅报告覆盖写入主报告路径

**完成标记：** checkpoint.nextStep = 2，写入日志。

---

### 步骤 2：双轴评分读取与强制 IF/THEN 分流

**调用 skill：** 无（编排器自执行）；加载 `../../写作研究/网文留存模型.md` 获取平台权重配置

**扮演角色：** 质检员角色。不代入任何创作或审阅视角，只做评分提取、双轴比较与循环调度决策。

**职责：** 从主审阅报告中提取技法分与留存分，根据以下 IF/THEN 规则执行强制分流。**本步骤的唯一职责是提取双轴评分 → 按双轴门槛做前置过滤 → 执行 IF/THEN 规则 → 根据结果分流，不得擅自改变分流目标。**

**【前置过滤：双轴各达标检查】**
提取报告中的双轴评分：

```text
技法分 = X.X/10（从报告中 `- 技法分：X.X/10` 提取）
留存分 = X.X/10（从报告中 `- 留存分：X.X/10` 提取）
```

前置过滤规则：
```
IF 技法分 < 6.0 或 留存分 < 5.0：
   → 标记为"双轴前置过滤未通过（技法=XXX/留存=XXX）"
   → 强制进入步骤 3（改稿），不检查综合评分
   → 必须优先修复不达标轴

IF 技法分 ≥ 6.0 且 留存分 ≥ 5.0：
   → 标记为"双轴前置过滤通过"
   → 进入综合评分 IF/THEN 分流
```

**【强制 IF/THEN 闸口规则（前置过滤通过后执行）】**
```
IF 综合评分 <= 9.2：
   → 你 MUST 进入步骤 3（改稿），不得进入步骤 6
   → 修改后必须进入步骤 4（复审），不得跳步

IF 综合评分 > 9.2 且这是 首轮 达标（前一循环 <= 9.2）：
   → 标记为"首次过线，进入稳定性验证轮"
   → 你 MUST 进入步骤 3（改稿），不得进入步骤 6
   → 修改后必须进入步骤 4（复审），不得跳步

IF 综合评分 > 9.2 且这是 连续第二轮 达标（前一循环也 > 9.2）：
   → 允许进入步骤 6（完成回执）
```

**硬性规则（违反即为流程违规）：**
- 9.2 不算通过，必须**严格大于** 9.2。
- 综合评分取值为保留两位小数后的数值。
- **双轴前置过滤优先于综合评分检查**——技法分 < 6.0 或留存分 < 5.0 时，不检查综合评分，直接进入步骤 3。
- 若竞对维度的量化结论仍有关键显著落后项（单维度 < 7.0/10），即使综合评分表面 > 9.2，也不得过线（强制进入步骤 3）。
- 步骤 2 的输出只有一个：进入步骤 3（改稿）或步骤 6（收尾）。不得在步骤 2 结束后擅自跳步或改变执行顺序。

1. 从主审阅报告中定位 `- 技法分：X.X/10` 和 `- 留存分：X.X/10`
2. 执行双轴前置过滤检查
3. 若通过，从主审阅报告中定位 `- 综合评分：X.X/10`，提取数值 X.X
4. 按上述 IF/THEN 规则执行分流

**完成标记：** checkpoint.nextStep = 3（改稿）或 6（收尾），写入日志。

---

### 步骤 3：按审阅报告修改提纲

**调用 skill：** `通用-设计总大纲` 或 `通用-设计分卷大纲`（及对应的题材包装层），按**对象路由规则**选择；按需调用 `通用-设计标题`、`通用-设计故事面`、`通用-设计事件案件引擎`、`通用-设计线索伏笔与回收台账`（及对应的题材包装层）

**扮演角色：** 小说作者角色（修订模式，参考 `.github/agents/小说作者.agent.md`）。**退出审阅者立场**，切换到作者视角。审阅报告是修改依据，修改以"解决问题"为目标，不是以"回应每一条意见"为目标。修改前重新阅读上级分配文件，确保修改方向不偏离上级分配。

**职责：** 根据最新审阅报告中的问题、修改建议与优化思路，对提纲做针对性修改。

**硬性规则（违反即视为流程违规）：**
- 必须直接写回目标提纲文件，不得只在聊天里给建议版。
- 修改必须覆盖上一轮核心扣分项（特别是阻断点/风险）。
- 必须吸收审阅报告中的问题、修改建议与"冲击 9.2+ 的优化思路"。
- 若存在竞对分析资料，修改必须至少落地 1 条"竞对借鉴动作"并补齐 1 条"竞对落后项"。
- 不得引入新的重大口径冲突。
- **步骤 3（修改）完成后，必须进入步骤 4（复审）。修改 → 复审 是强制配对，不得改完不审，也不得审了不改直接进入下一轮。**

**专项强化诊断与调用**（按需执行，不强制全部调用）：

| 诊断问题 | 调用 Skill | 处理范围 |
|---|---|---|
| 各级标题（卷名/章名等）抓力不足、与平台风格不匹配 | 通用-设计标题（及对应的题材包装层） | 对应标题层级的重新设计 |
| 故事面供血不足、卷级承接关系发虚、硬抓手不清晰 | 通用-设计故事面（及对应的题材包装层） | 对应故事面文件或总纲中故事面章节 |
| 单元案/事件引擎结构不成立、主线供血不足、规则显影不到位 | 通用-设计事件案件引擎（及对应的题材包装层） | 对应单元案文件或分卷中案件设计章节 |
| 线索台账/伏笔台账缺失、回收窗口不明确、推理公平性不足 | 通用-设计线索伏笔与回收台账（及对应的题材包装层） | 对应线索/伏笔台账文件或分卷中线索章节 |

1. 按对象路由规则加载对应的主设计 Skill（及题材包装层），代入作者角色
2. 读取当前版本的目标提纲 + 最新审阅报告 + 上级分配文件
3. 逐条对照审阅报告中的阻断点、风险项与扣分项，判断问题所属层级：
   - **[标题层]**：卷名/章名抓力不足、风格不匹配 → 调用通用-设计标题（及对应的题材包装层）
   - **[故事面层]**：供血不足、卷级承接发虚 → 调用通用-设计故事面（及对应的题材包装层）
   - **[事件/案件层]**：事件引擎不成立、主线供血不足 → 调用通用-设计事件案件引擎（及对应的题材包装层）
   - **[线索/伏笔层]**：台账缺失、回收窗口不明确 → 调用通用-设计线索伏笔与回收台账（及对应的题材包装层）
4. 针对性地按需调用上述一个或多个 Skill 执行专项修改
5. 修改后的提纲直接**覆盖写入**原目标提纲文件

**文件写入：** 修改后提纲覆盖写入原目标提纲文件

**完成标记：** checkpoint.nextStep = 4，写入日志。

---

### 步骤 4：复审（强制完整模板 + 差异对比）

**调用 skill：** `通用-审阅总大纲` 或 `通用-审阅分卷大纲`（及对应的题材包装层），按**对象路由规则**选择。

**扮演角色：** **切回审阅者角色（参考 `小说审阅者.agent.md` 的审阅立场）**。完全退出作者立场，以独立审阅者视角重新对修订版提纲进行全面审阅。本轮不仅检查上一轮问题是否修复，还要重新发现修改引入的新问题。标准不因"已经改过一轮"而降低。

**硬性规则（违反即视为流程违规）：**
- 必须是**对当前修订版本的完整、从严、独立的重新审阅**，而非只检查旧问题是否已修复。
- 不得以"与上一轮相比有进步"替代独立评分。
- 若存在竞对分析资料，必须重新判断竞对维度的领先点/落后点变化。
- 禁止通过"降低审阅口径"换取高分。
- **审阅报告必须使用完整模板**，不得写简版。至少需要包含步骤 1 规定的所有板块 + "与上一轮审阅的差异对比"。

**【强制门禁：审阅报告模板完整性检查】**
写入后、宣告完成前，逐项验证报告是否包含完整模板的所有板块 + 差异对比说明。缺失任一 → 判定为简版，回退重写。

1. 按对象路由规则加载对应的审阅 Skill（及题材包装层），代入审阅者角色
2. 读取修改后的目标提纲 + 上一版审阅报告（用于对比）+ 上级分配文件 + 相关裁判源
3. 执行全面审阅，使用完整审阅报告模板
4. 对照上一轮问题给出差异说明（已解决/部分解决/未解决/新增问题）
5. 报告中必须包含双轴评分：
   - `- 技法分：X.X/10`
   - `- 留存分：X.X/10`
   - `- 综合评分：X.X/10（权重：技法W_+留存W_=平台名）`
6. 若存在竞对分析资料，必须更新竞对量化模块（含留存竞对比对）
7. 审阅报告**覆盖写入**同一主报告路径
8. 执行模板完整性检查，确认无缺失

**文件写入：** 审阅报告覆盖更新到主报告路径

**完成标记：** checkpoint.nextStep = 2（回到评分读取与分流），写入日志。

---

### 步骤 5：循环控制（嵌入在步骤 2→3→4→2 中）

本步骤不是独立操作步骤，而是对 2→3→4→2 循环的约束规则：

- 回炉-审阅循环默认最多执行 **8 轮**（不含首次审阅 步骤 1）。超过 8 轮仍未达标时：
  1. 暂停循环，评估是否需更换模型、调整提示词口径或补充裁判源
  2. 第 8 轮评分若反而低于第 7 轮，强制截停
  3. 若连续两轮评分差异极小（浮动不超过 0.15），判定进入高原期，强制截停
  4. 不得单纯重复"改→审"循环
- "首次过线后追加稳定性验证轮"不计入 8 轮限制——验证轮是因达标而附加的必要步骤，不是因未达标而继续回炉。

---

### 步骤 6：完成回执输出

**调用 skill：** 无（编排器自执行）

**扮演角色：** 编排器 / 项目经理角色。不代入任何设计或审阅视角，只做数据汇总、一致性核对与格式化输出。

**职责：** 在闭环完成时，用标准化模板输出完成回执，确认连续两轮均过线。

**硬性规则：**
- **唯一收尾闸门**：只有在本步骤，才允许使用"闭环完成""过线收口"等收尾表述。
- 输出前必须逐项核对：目标提纲是否为最新修改版本、主审阅报告是否为最新复审版本、评分是否连续两轮 > 9.2。
- **严禁输出提纲正文内容**（含摘抄、长引用）；只能输出检查结果与路径。

**输出模板：**

```
大纲审阅优化闭环完成回执：
- 目标提纲：{OUTLINE_FILE}
- 对象类型：{总纲/分部/分卷/单元案}
- 主审阅报告：{REVIEW_REPORT_FILE}

过线记录：
- 倒数第二轮：技法分={X1}/10 | 留存分={Y1}/10 | 综合={Z1}/10
- 最终轮：技法分={X2}/10 | 留存分={Y2}/10 | 综合={Z2}/10
- 双轴达标（技法≥6.0 / 留存≥5.0）：{是/否}
- 连续两轮 > 9.2：{是/否}

修改记录：
- 总修改轮次：{N} 轮
- 末轮修改已覆盖核心扣分项：{是/否}
- 末轮留存问题已修复或分流至对应阶段：{是/否}

竞对维度（若有）：
- 领先点与补丁方向已对齐：{是/否}

需要进一步操作（如有）：{列出 1-3 条最重要的下一步}
```

**文件写入：** 日志 status = completed，最终 checkpoint 写入。

## SOP 执行日志

每次执行本 Skill，都必须在 SOP执行日志/大纲审阅/ 中创建或更新一份日志文件，用于断点恢复。

日志至少包含：executionId、目标提纲路径、对象类型、主报告路径、checkpoint（nextStep/phase/nextAction）、每轮回炉的 iteration 与评分纯数字、最终完成状态。

## 完成定义

只有同时满足以下全部条件，才允许宣告闭环完成：

1. 步骤 0→4 已在本次执行中实际跑完，而非依赖历史文件推断。
2. 目标提纲已被本次最新修改轮覆盖更新。
3. 主审阅报告已被本次最新复审覆盖更新。
4. 连续两轮独立审阅综合评分均严格大于 9.2。
5. 若竞对维度存在，竞对量化卡无关键显著落后项（单维度 >= 7.0/10）。
6. 日志中的 status 与 checkpoint 已与真实状态一致。
7. 当前聊天输出的是步骤 6 的完成回执模板，而非阶段汇报。

## 常见触发词 / 用户说法速查

- 把这份大纲审到过线
- 一直回炉到 9.2+
- 用同一份报告循环复审
- 这份总纲/分卷反复改到过线
- 给我一个大纲优化闭环

## 不适用情形

- 只做一次性审阅、不改稿：优先使用通用-审阅总大纲 或 通用-审阅分卷大纲
- 只新建大纲、不跑审阅闭环：优先使用通用-设计总大纲 或 通用-设计分卷大纲
