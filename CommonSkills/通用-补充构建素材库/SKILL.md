---
name: 通用-补充构建素材库
description: "用于在已有素材库目录（默认：项目根/素材库/）中定向补充、扩写或新建素材条目。适合素材库缺件诊断、定向搜索补充、按格式落盘、同步更新总索引与搜索日志的全流程闭环操作。关键词：补充素材、素材库缺件、素材收集、桥段池补全、人物设定填充、名场面扩写、搜索补素材、素材格式标准化。"
argument-hint: "要补充哪个素材池？缺什么类型/维度的素材？当前项目路径？若未指明目标池或缺口，先检查总索引（00_素材库总索引.md）各池素材密度与覆盖方向，给出缺件报告后再执行补充。"

---

# 通用-补充构建素材库

> 本 Skill 负责在已有素材库中执行"缺件诊断→定向搜索→格式落盘→索引更新→写入搜索日志"的全流程闭环。
> 素材库的构建理念、分类体系与方法论详见 `CommonSkills/写作研究/写作技法_网文作者素材库构建完全指南.md`。

## 硬规则（强制）

### 素材库目录约定

本 Skill 默认素材库位于 `素材库/`（项目根目录下），结构约定如下：

```text
素材库/
├── 00_素材库总索引.md              # 总索引（必须维护）——含题材交叉索引与情绪锚索引
│
│   ┌─ 【素材池区】写作时直接翻 ──┐
│   │                              │
├── 01_情节桥段池/                  # ★ 可直接用于写作的素材
│   ├── A_职场商战桥段.md
│   ├── B_家族内斗桥段.md
│   ├── C_关系推进桥段.md
│   └── ...                         # 当某子类超 100 条 → 拆分为子目录
├── 02_名场面高光池/                # ★
│   └── F_名场面高光池.md
├── 03_人物设定池/                  # ★
│   └── (子文件参考"深化规则")
├── 04_轻悬疑反转池/                # ★
│   └── D_轻悬疑桥段池.md
├── 05_冷幽默毒舌池/                # ★
│   └── E_冷幽默毒舌池.md
│   │                              │
│   └─────── 扩展方向 ① ──────────┘
│
│   ┌─ 【底稿区】研究参考 ────┐
│   │                          │
├── 竞品拆解/                     # ⓘ 竞品分析底稿
│   ├── 封总S1_分析.md
│   └── ...
├── 搜索日志/                      # ⓘ 搜索溯源
│   ├── 01_初始搜索汇总.md
│   └── ...
│   │                          │
│   └──────────────────────────┘
│
├── [题材名]/                      # 扩展方向 ②：题材级子素材库
│   ├── 该题材_索引.md
│   ├── 01_情节桥段池/             # 可复用通用 01 池结构，但子类去编号前缀，用题材名直接命名
│   ├── 02_名场面高光池/           # 同上，不适用则跳过（见"结构适配门禁"）
│   ├── 03_人物设定池/             # 同上
│   ├── 04_轻悬疑反转池/           # 同上
│   ├── 05_冷幽默毒舌池/           # ①─⑤ 仅为示例，并非每个题材都需要全部
│   ├── 06_空间场景库/             # ⚡ 题材新增池（悬疑/惊悚专用）——按需启用，编号顺延
│   ├── 07_故事设定档案库/         # ⚡ 题材新增池（科幻/赛博庞克专用）
│   ├── ...                        # 更多题材专属池——见"题材所需池类型推断"
│   ├── 竞品拆解/
│   └── 搜索日志/
│
└── [另一题材名]/                  # 每个题材一个同级子素材库
    └── ...
```

**三轴设计理念**：
- **扩展轴（题材层）**：当一个题材积累的素材量超过通用01-05池的覆盖范围（参见"可扩展——题材层"规则），在同级创建 `题材名/` 作为子素材库
- **深化轴（文件分裂）**：单文件超 100 条时，按子类拆分为子目录+多个文件（参见"可深化——文件分裂"规则）
- **搜索轴（情绪锚索引）**：`00_素材库总索引.md` 末尾维护跨池的情绪锚汇总表，配合 `grep_search` 快速定位（参见"可搜索——情绪锚索引"规则）

---

### 可扩展——题材层

> **问题**：01-05 通用池服务于当前小说的情节供血。但当你开新题材（如从都市职场转悬疑推理），不同题材的情绪锚/节奏/人设差异巨大，混在同一个池里互相干扰。更根本的问题是：**01-05 的分类逻辑本身来自女频都市情感题材，并非放之所有题材而皆准。**

因此在新建题材子库之前，必须先做**结构适配门禁**检查，确定通用池分类对该题材是否有效、哪些池需要替换、哪些池需要新增。

---

#### 结构适配门禁（新建题材子库前的必检项）

> **核心原则**：题材子库不是通用 6 池的简单复制。每个题材应该拥有匹配自身叙事特征的自有池体系。

**检查方法**：逐池回答以下问题，记录"保留/替换/禁用/新增"决策：

| # | 池名称 | 检查问题 | 决策 | 理由 |
|---|--------|---------|------|------|
| 01 | 情节桥段池 | 通用池中 A/B/C（职场商战/家族内斗/关系推进）的分类逻辑是否覆盖该题材的核心冲突类型？ | 保留/替换子类/禁用 | — |
| 02 | 名场面高光池 | 该题材是否有"可供读者截图传播的高光时刻"？其情绪递进节奏是否与通用池一致？ | 保留/禁用 | — |
| 03 | 人物设定池 | 该题材的人物类型是否超出"霸总/女主/反派/配角"框架？ | 保留/替换子类/禁用 | — |
| 04 | 轻悬疑反转池 | 该题材的悬念是靠"情感真相揭露"驱动，还是靠其他方式（世界之谜/案件推理/技术诡计）？ | 保留/替换/禁用 | — |
| 05 | 冷幽默毒舌池 | 该题材是否需要大量嘴炮/毒舌/金句供血？ | 保留/禁用 | — |

**典型题材的门禁决策参考**：

| 题材 | 01 情节 | 02 名场面 | 03 人物 | 04 悬疑 | 05 毒舌 | 建议新增池 |
|------|---------|-----------|---------|---------|---------|-----------|
| 女频爱情 | ✅ 保留（替换子类为追妻/拉扯/双向） | ✅ 保留 | ✅ 保留 | ✅ 保留 | ✅ 保留 | — |
| 都市职场 | ✅ 保留 | ✅ 保留 | ✅ 保留子类（去霸总） | ⚠️ 保留（合同/商业欺诈向） | ✅ 保留 | — |
| 都市悬疑 | ✅ 保留（替换子类为案件/调查/嫌疑） | ✅ 保留 | ✅ 保留（替换为刑警/受害人/嫌疑人） | ✅ 保留（案件推理向） | ✅ 保留 | **06_空间场景库** |
| 悬疑推理 | ⚠️ 保留（替换为谜题/推演/作案手法） | ⚠️ 保留（高光在解谜瞬间而非打脸） | ✅ 保留（替换为侦探/助手/凶手/嫌疑人） | ✅ 保留（逻辑推演主导） | ❌ 禁用 | **06_空间场景库**、**07_诡计手法库** |
| 异能志怪 | ✅ 保留（替换为能力冲突/异界事件） | ✅ 保留 | ✅ 保留（替换为能力者/灵异体质/普通视角） | ✅ 保留（超自然谜团向） | ✅ 保留 | **06_能力设定库**、**07_诡异氛围库** |
| 太空科幻 | ⚠️ 保留（替换为战略博弈/飞船遭遇/外交冲突） | ✅ 保留 | ⚠️ 保留（替换为舰长/工程师/外星生物） | ✅ 保留（宇宙之谜/技术阴谋） | ❌ 禁用 | **06_故事设定档案库**、**07_星舰/空间站场景库** |
| AI科幻 | ⚠️ 保留（替换为人机冲突/伦理博弈） | ⚠️ 保留（高光在认知冲击而非打脸） | ❌ 禁用（替换为AI/人类关系模板） | ✅ 保留（技术谜团/AI动机黑箱） | ❌ 禁用 | **06_故事设定档案库**、**07_人机关系模板库** |
| 赛博庞克 | ✅ 保留（替换为义体黑市/数据窃取/街头火拼） | ✅ 保留 | ⚠️ 保留（替换为黑客/义体师/公司战士） | ✅ 保留（公司阴谋/身份谜团） | ✅ 保留（底层讽刺/黑色幽默） | **06_故事设定档案库**、**07_义体改造与赛博配件库** |

> 注：✅=保留（按题材替换子类）｜⚠️=部分调整后保留｜❌=禁用，不在题材子库中建立对应池

**执行规则**：
1. 新建题材子库前，必须先输出一张结构适配门禁表，记录逐池判断结果
2. 门禁表必须写入 `题材名_索引.md` 的"结构适配决策"段，供后续补充者参考
3. 被禁用的池直接跳过（不在题材子库中创建空目录占位）
4. 门禁表将来若发现原判断不合理，可修订并更新索引中的记录

---

#### 触发条件（扩展后）

出现以下**任一**情况时，应在 `素材库/` 同级下新建 `题材名/` 子素材库：

**数量驱动**（原规则，继续有效）：
- 当前项目切换到不同题材（如从"都市职场"切到"太空科幻"）
- 通用池中"与当前题材无关"的条目数超过 30%（说明池已被稀释）
- 同一题材的素材总量超过 200 条，且仍在快速增长

**结构驱动**（新增规则）：
- 通过"结构适配门禁"检查后，发现 **≥ 2 个池需要替换子类**，且替换幅度超过子类总量的 50%——说明通用池的分类轴已经偏移，不值得在通用层修补，应直接建题材子库
- 需要新增 **≥ 1 个当前 01-05 体系完全不存在的池类型**（如科幻需要"故事设定档案库"）——说明通用层不提供该维度的分类位置
- 题材的**核心叙事驱动力**与通用池设计时的驱动力不同（如通用池为"情感驱动力"设计，新题材为"解谜驱动力"或"世界观探索驱动力"）——说明情绪锚索引体系不通用

---

#### 题材所需池类型推断

> 新建题材子库时，不照搬通用 01-05 编号——而是按题材特征推断"这个题材需要哪些池"。

**推断维度**：

| 题材特征 | 需要的池类型 |
|---------|-------------|
| 冲突依赖外部事件/案件驱动 | → 事件/案件引擎库（可映射为 01 情节 -> 替换子类） |
| 冲突依赖人际关系/情感张力 | → 关系推进库、对话冲突库（可映射为 01C/01B） |
| 高光时刻依赖情绪爆发/反转/打脸 | → 名场面高光池（保留 02） |
| 高光时刻依赖认知冲击/解谜瞬间/设定震撼 | → 名场面高光池（保留 02，但分类逻辑替换为"解谜高光""设定揭示"等） |
| 人物是驱动核心 | → 人物设定池（保留 03） |
| 人物是世界观载体（如异能、赛博改造） | → 人物设定池（保留 03，但需增加能力/改造维度字段） |
| 有技术/规则/系统体系需要维护 | → **故事设定档案库**（新增 06）——存放技术原理、规则条款、程序链、证据载体 |
| 有空间/场景/氛围的叙事需求 | → **空间场景库**（新增 06/07）——存放典型空间的感官描述、氛围清单、空间剧本 |
| 需要离奇/灵异/超自然的气氛 | → **诡异氛围库**（新增）——存放氛围描述、异常征兆、感官细节 |
| 需要系统化的诡计/手法/谜题 | → **诡计手法库**（新增）——存放推理诡计、谜题设计、误导与线索编排 |
| 需要大量毒舌/金句/冷幽默调节节奏 | → 冷幽默毒舌池（保留 05） |
| 不需要毒舌调节（严肃/压抑/学术风格） | → 冷幽默毒舌池（禁用） |

**新增池的编号规则**：
- 通用 01-05 保留为通用层体系，不以题材层的加入而变更
- 题材子库中新增的池，编号从 **06 开始顺延**（无论通用层有多少池）
- 允许跳号，允许不在题材层中出现 01-05 中的某几个池（被禁用的池直接省略，不保留空编号）
- 编号在题材的 `_索引.md` 中重新声明，不与通用层的编号互相引用

---

#### 题材子库结构（灵活版）

以下展示两种题材子库的差异——**不是复制通用 6 池，而是按题材需求裁剪**：

```text
素材库/
├── (通用 00_总索引 + 01-05 池）
│
├── 女频爱情/                    # 示例 A：6 池基本套用，子类替换
│   ├── 女频爱情_索引.md
│   ├── 01_情节桥段池/           # 子类：追妻/拉扯/破冰/双向伤害
│   ├── 02_名场面高光池/
│   ├── 03_人物设定池/           # 子类：霸总/女主/恶配角/关系模板
│   ├── 04_轻悬疑反转池/         # 情感向：合同谜团/身世秘密
│   ├── 05_冷幽默毒舌池/         # 女主怼人金句
│   ├── 竞品拆解/
│   └── 搜索日志/
│
├── 太空科幻/                    # 示例 B：需禁用+新增若干池
│   ├── 太空科幻_索引.md
│   ├── 01_情节桥段池/           # 子类：舰船遭遇/外交博弈/技术伦理
│   ├── 02_名场面高光池/         # ⚠️ 分类逻辑改为"文明认知冲击/技术反转"
│   ├── 03_人物设定池/           # 子类：舰长/科学家/AI/外星生物
│   ├── 04_轻悬疑反转池/         # 宇宙之谜/外星遗迹/AI动机
│   │                            # 05_冷幽默毒舌池 → 禁用（不保留目录）
│   ├── 06_故事设定档案库/       # ⚡ 新增池：跃迁引擎/外星科技/生态环系统
│   ├── 07_星舰场景库/           # ⚡ 新增池：舰桥/冷冻舱/气闸/异星地表
│   ├── 竞品拆解/
│   └── 搜索日志/
│
├── 悬疑推理/                    # 示例 C：核心池为谜题驱动
│   ├── 悬疑推理_索引.md
│   ├── 01_情节桥段池/           # 子类：发案/排查/审讯/推翻/收网
│   ├── 02_名场面高光池/         # 分类：解谜瞬间/当面对质/真凶揭晓
│   ├── 03_人物设定池/           # 子类：侦探/助手/嫌疑人/警方/凶手
│   ├── 04_轻悬疑反转池/         # 逻辑推演向：线索链/误导/时间线矛盾
│   │                            # 05_冷幽默毒舌池 → 禁用
│   ├── 06_空间场景库/           # ⚡ 新增池：犯罪现场/审讯室/追逃空间
│   ├── 07_诡计手法库/           # ⚡ 新增池：密室/不在场证明/伪证/模拟犯罪
│   ├── 竞品拆解/
│   └── 搜索日志/
│
└── 赛博庞克/
    ├── 赛博庞克_索引.md
    ├── 01_情节桥段池/           # 子类：数据窃取/街头火拼/公司暗战/基因黑市
    ├── 02_名场面高光池/
    ├── 03_人物设定池/           # 子类：黑客/义体师/公司武士/AI觉醒体
    │                            # 04_轻悬疑反转池 → 替换方向为"公司阴谋"
    │                            # 05_冷幽默毒舌池 → 保留（黑色幽默/底层讽刺）
    ├── 06_故事设定档案库/       # ⚡：神经接入/义体规格/数字空间协议
    ├── 07_赛博配件与改造库/     # ⚡：各类义体/植入物/改造后遗症
    ├── 竞品拆解/
    └── 搜索日志/
```

---

#### 题材子库规则

1. **入口文件**：每个题材子库必须有一个 `题材名_索引.md`，格式参照 `00_素材库总索引.md`，此外额外包含"结构适配决策"段（记录门禁检查表）
2. **编号去前缀**：题材子库的子文件不使用字母前缀（如 `A_职场商战桥段.md`）——直接以意义命名（如 `跃迁引擎事故.md`），避免与通用池的编号体系冲突
3. **优先查子库**：写作时先查当前题材的子库，再回退到通用池。题材子库中不存在的通用桥段，用通用池补位
4. **新建后注册**：题材子库建好后，必须在 `00_素材库总索引.md` 末尾"子素材库注册表"中登记（路径+创建日期+覆盖池列表）
5. **情绪锚索引改为题材私有**：情绪锚索引在 `题材名_索引.md` 中独立维护，不再放到通用总索引中，避免跨题材的情绪锚混在一起失效
6. **通用池只增不减**：题材子库的新增池、禁用池只影响题材自己的素材操作，通用 01-05 池永远保留不动

---

### 可深化——文件分裂

> **问题**：单文件持续追加条目（如职场商战 80+ 条、冷幽默 100+ 条），迟早会因文件过长导致查找困难和编辑性能下降。

**分裂阈值**：

| 计量指标 | 触发分裂 | 建议动作 |
|----------|---------|---------|
| 单文件条目数 | 超过 **100 条** | 拆为子目录 |
| 单文件行数 | 超过 **500 行**（不含空行与分隔线） | 拆为子目录 |
| 文件内子类数 | 超过 **5 个 `##` 二级标题** | 每个二级标题拆为单独文件 |

**分裂操作规范**：

以当前 `A_职场商战桥段.md`（≈80+条，含 A01-A04 四个子类）为例：

```text
分裂前：                        分裂后：
01_情节桥段池/                  01_情节桥段池/
├── A_职场商战桥段.md   →       ├── A_价格博弈与竞标/
│                               │   ├── _index.md        # 本目录索引
│                               │   ├── 01_沉默战术.md
│                               │   └── ...
│                               ├── B_会议对决与职场博弈/
│                               │   ├── _index.md
│                               │   └── ...
│                               └── C_行业声誉与危机管理/
│                                   ├── _index.md
│                                   └── ...
```

**分裂硬规则**：
1. **保留入口文件**：原文件保留为入口，内容替换为本目录的文件列表索引（`_index.md` 格式），各子文件条目的编号前缀保持不变
2. **`_index.md` 格式**：

```markdown
# [池名]

> **原文件**：`原文件名.md`（已分裂为子目录，YYYY-MM-DD）
> **总条目**：[N] 条

| 子文件 | 内容 | 条目数 |
|--------|------|--------|
| 01_沉默战术.md | 价格博弈与竞标对决 | 20+ |
| 02_会议对决.md | 会议博弈与职场对决 | 15+ |
| ... | ... | ... |
```

3. **全局编号不变**：子文件内的条目编号（如 `A01-07`）继承原文件体系，不重新编号
4. **子目录名称**：用原二级标题的缩写，最多 8 个中文字符
5. **分裂后更新总索引**：在 `00_素材库总索引.md` 的"文件结构变更记录"表中登记分裂操作

---

### 可搜索——情绪锚索引

> **问题**：素材散在多个池的多个文件中，写某一章时需要"一个爽的打脸场面"——但不知道它在哪个池、哪个文件里。

**方案**：在 `00_素材库总索引.md` 末尾维护一张**跨池的情绪锚索引表**。

**情绪锚索引表格式**：

```markdown
## 情绪锚索引

| 情绪锚 | 条目示例（池/编号/标题） | 池 | 数量 |
|--------|-------------------------|----|------|
| 爽     | 01A｜A01-01 用对手漏洞反制 | 情节桥段池-职场商战 | 30+ |
| 爽     | 02｜F01 签字离婚           | 名场面高光池 | 20+ |
| 心理战 | 01A｜A01-03 沉默战术       | 情节桥段池-职场商战 | 15+ |
| 悬疑   | 04｜D01-01 旧书里的合同    | 轻悬疑反转池 | 50+ |
| 毒舌   | 05｜E01-01 女主→男主       | 冷幽默毒舌池 | 100+ |
| ...    | ...                       | ... | ... |
```

**构建与维护规则**：
1. **初始构建**：在每次大规模补充后，由助手扫描各池文件的 `**情绪锚**：` 字段，汇总去重后写入此表
2. **索引条目的条件**：情绪锚出现频次 ≥ 3 次的才入索引（低频锚不值得维护）
3. **维护时机**：每次补充落盘 + 总索引更新时，同时更新此表
4. **配合 grep 搜索**：写作时，在 VS Code 中按 `Ctrl+Shift+F`，用情绪锚关键词搜索即可跨池定位：

```text
# 示例：搜索所有"心理战"标签的素材
Ctrl+Shift+F → grep: "情绪锚.*心理战" → 路径: 素材库/
```

5. **路径级精准搜索**：如果需要限定池：

```text
# 只在悬疑池中搜"证据"
grep: "证据形态" → 路径: 素材库/04_轻悬疑反转池/
```

6. **索引不做实时同步**：素材条目持续增加，索引在每次补充操作时批量更新即可，不需要每次新增一条就同步一次

### 条目录入格式规范

所有素材池中的条目必须遵循以下字段结构。**缺字段的条目不得入库**。

| 字段 | 必填 | 说明 | 示例 |
|------|------|------|------|
| **标题** | 是 | 精炼概括本条素材的名称 | "用对手的漏洞反制对手" |
| **字数** | 是 | 该素材展开成文的预计字数范围 | "3000-5000字" |
| **情绪锚** | 是 | 核心情绪标签（爽/虐/燃/悬疑/催泪/暗爽/心理战等） | "专业碾压" |
| **梗概** | 是 | 1-3段话描述情节/场景/冲突的完整轮廓 | "招标会上，对手用苏棠上一家客户的数据来攻击她…" |
| **设计逻辑** | 推荐 | 该素材为什么有效，核心设计意图 | "沉默是最有力的谈判武器——让对方先沉不住气" |
| **适配故事面** | 推荐 | 该素材对应哪个故事面/M0/X线 | "X01 事业线" |

名场面高光池的条目**额外强制**包含 **金句** 字段——"读者想截图发朋友圈"的核心台词或旁白。

### 补充前必须做缺件诊断

在任何补充动作之前，必须先读取 `00_素材库总索引.md`，检查缺件方向：

1. **素材密度对标**：对比各池当前条目数与总索引中声明的"各卷素材密度目标"，找出低于目标密度的池
2. **覆盖方向缺口**：总索引中"本轮大规模搜索覆盖"表记录了已覆盖的方向，未覆盖的方向即为搜索缺口
3. **搜索日志检查**：读取 `搜索日志/` 下的搜索记录，确认某方向是否已做过搜索、搜索质量如何
4. **空池检查**：检查各池文件是否为空或条目不足（如当前 `03_人物设定池/` 为空）

得到缺件清单后，再规划补充顺序和搜索策略。

### 网络搜索范式

补充素材时，搜索策略按素材池类型区分：

| 池类型 | 推荐搜索方向 | 关键词组合 |
|--------|-------------|-----------|
| 情节桥段（01） | 按场景/冲突类型搜 | "职场商战 经典谈判桥段 应对刁难 反转" |
| 名场面（02） | 按情绪锚搜 | "打脸名场面 爽文 高光时刻 女主逆袭 社交传播" |
| 人物设定（03） | 按人设/身份搜 | "霸总人设 经典性格 大佬细节描写 反派模板" |
| 悬疑反转（04） | 按手法类型搜 | "轻悬疑 反转手法 真相揭露 伏笔回收 合同谜团" |
| 冷幽默（05） | 按性别/身份搜 | "女主毒舌金句 怼人 冷幽默 女频嘴炮" |
| 竞品拆解 | 按平台+题材搜 | "起点 女频 总裁文 追妻火葬场 爆款 结构拆解" |

- 每次搜索需记录到 `搜索日志/`（格式见后）
- 每条搜索结果必须清洗成标准素材格式后才能入库
- 不得直接粘贴搜索结果原文作为素材条目（违反"条目录入格式规范"）

### 总索引必须同步更新

每次补充素材落盘后，必须更新 `00_素材库总索引.md`：

- 更新对应素材池的"素材条目"计数
- 在"本轮搜索覆盖"表中追加本次补充的搜索方向和产出条目数
- 更新"更新日期"为当天

### 搜索日志必须同步记录

每次执行网络搜索后，必须在 `搜索日志/` 下的对应文件中追加搜索记录行，格式如下：

```text
| [序号] | [搜索关键词] | [搜索工具] | [有效结果数] | [备注] |
```

若搜索量大，可在 `搜索日志/` 下新建 `03_搜索素材汇总.md` 等增量文件，但需在总索引中声明。

### 条目录入质量门禁

每条素材在落盘前必须通过以下自检：

- [ ] 字段完整（至少标题+字数+情绪锚+梗概填充）
- [ ] 梗概有画面感：不是一个抽象概念，而是读者能直接想象的场景
- [ ] 情绪锚明确：读者知道读了这段是什么情绪预期
- [ ] 有区分度：不是"不说也知道"的套路描述，而是有具体细节/反转/金句
- [ ] 不与同池已有条目雷同（检查同池文件）
- [ ] 情绪锚/字数估算合理（字数估算偏差不超过 30%）

---

## 保真迁移要求（强制）

命中素材库补充任务时，除本文件外，**必须继续读取**：

- `references/各池条目录入模板.md` — 各池专属的条目录入模板与示例
- `references/缺件诊断检查表.md` — 缺件诊断的完整检查列表与标准化结果格式
- `references/搜索日志记录模板.md` — 搜索日志的记录格式与字段定义
- `00_素材库总索引.md` — 素材库总索引（在执行目录下读取）
- `CommonSkills/写作研究/写作技法_网文作者素材库构建完全指南.md` — 素材库构建理念与方法论背景（当用户质疑或偏离方法论时须加载）

**本文件内涉及素材库架构的章节**（执行非架构变更的日常补充时不必重读）：
- `可扩展——题材层`（含结构适配门禁、题材所需池类型推断、灵活版子库结构）
- `素材库目录约定`（含题材层示例树）
- `可深化——文件分裂`
- `可搜索——情绪锚索引`
- `步骤 8：题材子库注册与结构注册`

---

## 执行流程

### 步骤 1：缺件诊断

用户明确指定补充目标时，跳到步骤2。

用户未指定时，执行缺件诊断：

1. 读取执行目录下的 `00_素材库总索引.md`
2. 读取各池文件，检查实际条目数与总索引声明的匹配度
3. 检查 `03_人物设定池/` 等池是否为空
4. 检查 `搜索日志/` 下已覆盖的方向与缺口
5. 输出缺件报告（格式参考 `references/缺件诊断检查表.md`），提交用户确认

### 步骤 2：用户确认补充方向

展示缺件报告后，与用户确认：

```
补充方向建议：
├── 优先补充：[池名] — [缺的具体方向]
├── 条目数量目标：[N] 条
├── 搜索关键词：[初步搜索关键词]
├── 情绪锚覆盖：[情绪类型列表]
└── 确认 / 调整？
```

### 步骤 3：定向搜索

1. 按确认的补充方向，执行网络搜索
2. 清洗搜索结果，提取可用条目
3. 每执行一次搜索，立即记录到搜索日志

### 步骤 4：条目格式化与落盘

1. 将清洗后的条目按标准格式写入对应的池文件
2. 引用同池已有条目的编号（如果存在继发关系，如 `A01-06 基于A01-01的变体`）
3. 新条目使用池内递增编号

### 步骤 5：总索引更新

1. 更新 `00_素材库总索引.md` 中的素材计数
2. 在"本轮搜索覆盖"表中追加本次补充的搜索方向与产出
3. 更新总索引的"情绪锚索引"表（去重汇总本次新条目的情绪锚，频次 ≥ 3 的入索引）
4. 更新总索引的"更新日期"

### 步骤 6：搜索日志归档

1. 如果本次搜索量 < 10 次：追加到 `搜索日志/` 下已有的日志文件中
2. 如果本次搜索量 ≥ 10 次：新建 `搜索日志/NN_增量搜索_YYYYMMDD.md`，同时在总索引中声明

### 步骤 7：最终摘要

向用户提交本次补充的最终摘要：

```text
## 补充摘要
- 目标池：[池名]
- 补充条目：[N] 条
- 覆盖情绪锚：[情绪锚列表]
- 搜索轮次：[N] 次
- 搜索来源：[来源摘要]
- 总索引已更新：是/否（含情绪锚索引更新：是/否）
- 搜索日志已归档：是/否（路径）
- 子素材库变更：新建/未变
```

### 步骤 8：题材子库注册与结构注册（仅新建时）

使用"可扩展——题材层"规则新建题材子库后执行：

**前置步骤 8a：执行结构适配门禁**
1. 按"结构适配门禁"逐池判断，输出结构适配决策表（保留/替换/禁用/新增）
2. 将决策表写入 `题材名_索引.md` 的"结构适配决策"段
3. 根据决策表创建实际目录——只建"保留"和"新增"的池，被禁用的池不占位

**步骤 8b：注册到总索引**
1. 在 `00_素材库总索引.md` 末尾"子素材库注册表"中登记一行
2. 格式如下：

```text
| [题材名] | [题材名]_索引.md | YYYY-MM-DD | 情节桥段/名场面/人物设定/悬疑/[禁用]/竞品拆解/搜索日志 | 06_故事设定档案库(新增) |
```

**步骤 8c：建立题材索引文件**
1. 创建 `题材名_索引.md`，格式与 `00_素材库总索引.md` 一致
2. 额外包含"结构适配决策"段（记录门禁检查表）
3. 情绪锚索引在该文件中独立维护，不入通用总索引

---

## 各池类型差异

### 01_情节桥段池（A/B/C）

- 每条必须包含清晰的情绪锚——读者预期读完这段的情绪
- 梗概必须包含具体人物、场景、对手、冲突、转折
- 设计逻辑可选但推荐——解释该桥段为什么有效

### 02_名场面高光池（F）

- 额外强制字段：**金句**
- 名场面必须达到"读者想截图发朋友圈"的传播级效果
- 优先收集"打脸反杀""催泪情感""反转震撼"三类

### 03_人物设定池

- 当前为空池，初次填充使用以下子类别结构：

```text
03_人物设定池/
├── A_霸总人设.md
├── B_女主设定.md
├── C_反派模板.md
├── D_配角原型.md
└── E_人物关系动态模板.md
```

- 人物条目的格式（区别于情节条目）：

| 字段 | 说明 |
|------|------|
| **人设标签** | 核心记忆点（如"极度信奉道教戒律的道士"） |
| **底层欲望** | 驱动这个人物的核心需求 |
| **核心矛盾** | 他/她与外部世界或自身的核心冲突 |
| **反差细节** | 让人物立体的反差行为 |
| **声口特征** | 标志性说话方式、用词习惯 |
| **适配故事面** | 可服务于哪些故事面/X线 |

### 04_轻悬疑反转池（D）

- 每条必须包含清晰的"发现→推导→揭晓"逻辑链
- 细节必须包含具体证据形态（实物/文件/照片/录音/行为异常等）
- 反转不能依赖"巧合"，必须有合理前置铺垫

### 05_冷幽默毒舌池（E）

- 每条必须适配目标人物的身份关系场景（怼男主/怼白月光/怼恶配角等）
- 金句标准：聪明不刻薄、犀利不伤人、一本正经地荒诞
- 优先收集"可三连击"的对话组——不是孤立一句话，而是有来有回的对话组合

### 竞品拆解

> 竞品拆解目录存放的是面向整部作品的分析底稿，**不是**可直接取用的素材条目。
> 竞品拆解中提取出的可复用桥段、手法、金句，应清洗后归入对应的01-05素材池。
> 若需直接制作素材条目，使用"通用-分析竞对作品"Skill。

- 条目采用"原文段落+拆解分析"的双段结构
- 拆解分析必须包括：作者用了什么手法、这段为什么抓人、可以借鉴什么
- 并非全文照搬，而是精选"可复用的技巧片段"

---

## 常见触发词 / 用户说法速查

| 用户说 | 本 Skill 做什么 |
|--------|----------------|
| "给素材库补充点XX" | 定向搜索+落盘+索引更新 |
| "XX池还缺什么" | 执行缺件诊断 |
| "帮我搜几个XX方向的素材" | 搜索+清洗+格式落盘 |
| "人物设定池怎么还是空的" | 初始化人物池+首次填充 |
| "总索引不一致" | 读取各池实际条目，校对总索引 |
| "这个条目格式不对" | 按本 Skill 规范重写条目 |
| "把这些搜索结果整理一下" | 清洗搜索记录→标准条目录入 |
